# Prompt 2 — Stop Logging

At the **end of the session**, paste everything below the line into the same
AI agent. It closes out `PROMPTS.md`. Send that file to your interviewers
afterward.

---

End interview and stop logging prompts.

Finalize `PROMPTS.md` now:

1. Append this message as the final numbered entry.
2. Check the log top to bottom: entries must be numbered sequentially with no
   gaps. If any prompt from this session is missing, recover it verbatim from
   the conversation and insert it in the correct position, marked
   `*(recovered)*`.
3. Append this footer:

   ```markdown
   ---

   **LOGGING ENDED** — <current date and time>
   **Total prompts:** <count>
   ```

4. Stop logging. Confirm to me that the log is complete and tell me the total
   prompt count.
